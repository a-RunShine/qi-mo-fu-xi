# 第八章 数据库编程（嵌入式 SQL）— 选择题

1. 以下关于嵌入式 SQL 中游标的说法，正确的是（  ）。
A. 游标是 C 语言中的一种指针类型，可以随机访问结果集中的任意行
B. 游标是 SQL 系统维护的结果集访问机制，只能逐行向前推进
C. 游标声明（DECLARE）时就会执行 SQL 语句并将结果集取出
D. 游标关闭后，结果集仍然保留在内存中
2. 以下嵌入式 SQL 程序片段中，存在错误的是（  ）。

```c
EXEC SQL BEGIN DECLARE SECTION;
char sno[9];
char sname[20];
EXEC SQL END DECLARE SECTION;
strcpy(sno, "S001");
EXEC SQL SELECT Sname INTO sname FROM Student WHERE Sno = sno;
```

A. 主变量 sno 在 SQL 语句中缺少冒号前缀
B. INTO 子句不能用于 SELECT 语句
C. sname 变量类型声明错误
D. 没有错误

3. 在嵌入式 SQL 中，SQLCODE 的值为 100 时表示（  ），此时应执行的操作是（  ）。
A. SQL 执行失败；回滚事务
B. 查询结果为空或 FETCH 完所有数据；退出循环
C. SQL 执行成功；继续 FETCH
D. 权限不足；断开连接

4. 以下关于指示变量的说法，正确的是（  ）。
A. 指示变量可以是任意数据类型
B. 指示变量的值为 0 时表示主变量值为 NULL
C. 指示变量的值大于 0 时表示主变量值被截断
D. 指示变量必须与主变量在同一个 DECLARE SECTION 中声明，且类型必须一致

5. 以下关于嵌入式 SQL 中连接管理的说法，错误的是（  ）。
A. 嵌入式 SQL 访问数据库前必须先建立连接
B. 一个程序可以同时建立多个数据库连接
C. 使用 EXEC SQL DISCONNECT 关闭连接后，可以重新 CONNECT
D. 连接一旦建立，在整个程序运行期间不能切换

6. 以下关于嵌入式 SQL 与主语言之间数据传递的说法，正确的是（  ）。
A. SQL 通信区（SQLCA）用于传递数据，主变量用于传递状态信息
B. 输入主变量由应用程序赋值供 SQL 使用，输出主变量由 SQL 赋值返回给应用程序
C. 游标用于传递单条记录，主变量用于传递多条记录
D. 指示变量用于指示 SQL 语句是否执行成功

7. 以下关于 WHERE CURRENT OF 子句的说法，正确的是（  ）。
A. WHERE CURRENT OF 可以用于任意 UPDATE 语句
B. WHERE CURRENT OF 用于更新当前游标指向的那一行
C. WHERE CURRENT OF 可以同时更新多行
D. WHERE CURRENT OF 不需要游标配合

8. 在嵌入式 SQL 中，以下关于事务控制的说法，正确的是（  ）。
A. COMMIT 提交事务后，所有未关闭的游标将被自动关闭
B. ROLLBACK 会撤销所有未提交的修改，但游标状态保持不变
C. 嵌入式 SQL 程序中，每个 FETCH 操作后必须执行 COMMIT
D. 嵌入式 SQL 程序不需要事务控制，由 DBMS 自动管理

9. 以下关于嵌入式 SQL 预编译过程的说法，正确的是（  ）。
A. EXEC SQL 关键字由 C 编译器直接识别并编译
B. 预编译器将嵌入式 SQL 语句替换为 DBMS 函数调用，再交给 C 编译器
C. 预编译过程是可选的，不预编译也可以运行
D. 预编译器只处理 SQL 语句，不处理主变量声明

10. 某嵌入式 SQL 程序需要从 Student 表中逐行读取数据，每读取一行就根据业务逻辑决定是否更新该行。以下实现方案中，最合理的是（  ）。
A. 使用一条 SELECT 语句将所有数据取出，在 C 语言中循环处理
B. 声明游标，逐行 FETCH，使用 WHERE CURRENT OF 更新当前行
C. 使用一条 UPDATE 语句一次性更新所有行
D. 为每一行单独执行一条 SELECT 和一条 UPDATE

---

## 答案汇总

1.B 2.A 3.B 4.C 5.D 6.B 7.B 8.A 9.B 10.B
